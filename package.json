{
  "name": "effect-arch-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Effect-TS architecture skills and mechanical enforcement for React + Effect apps.",
  "license": "MIT",
  "author": "codewithcats",
  "repository": {
    "type": "git",
    "url": "https://github.com/codewithcats/effect-arch-kit.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/**/test/*.mjs",
    "check:skill-rule-ids": "node scripts/check-skill-rule-ids.mjs",
    "check:residue": "node scripts/check-residue.mjs",
    "verify": "npm run check:residue && npm run check:skill-rule-ids && npm test"
  }
}
